// A .protect middleware that can be added to any route with a set of permissions instead of manually verifying. Also inserts the user data into the
// object without needing to manually call the getUser function.
